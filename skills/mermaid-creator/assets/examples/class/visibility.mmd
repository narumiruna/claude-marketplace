classDiagram
    class BankAccount {
        +String accountNumber
        -double balance
        #String ownerName
        ~int transactionCount
        +deposit(amount)
        +withdraw(amount)
        -calculateInterest()
    }
